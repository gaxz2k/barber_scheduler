<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="m-0">Agendamentos</h1>
  <%= link_to "Novo agendamento", new_admin_appointment_path, class: "btn btn-primary" %>
</div>

<table class="table table-striped align-middle">
  <thead>
    <tr>
      <th>Nome</th>
      <th class="text-end">Ações</th>
    </tr>
  </thead>
  <tbody>
    <% @appointments.each do |appointment| %>
      <tr>
        <td><%= appointment.client.name %></td>
        <td class="text-end">
          <%= link_to "Editar", edit_admin_appointment_path(appointment), class: "btn btn-sm btn-outline-secondary" %>
          <%= button_to "Excluir", admin_appointment_path(appointment), method: :delete, class: "btn btn-sm btn-outline-danger", form: { data: { turbo_confirm: "Tem certeza?" } } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
